/**
 * app/time-records.tsx — редирект на «Час».
 *
 * Окремого екрана «Записи часу» більше немає. Він читав ЛИШЕ
 * `task.timeEntries`, тоді як вкладка «Час» читала ЛИШЕ `time_entries`, — два
 * екрани про одне й те саме показували різні підсумки. Тепер джерело одне, а
 * фільтри, KPI і графіки, заради яких цей екран існував, живуть на самій
 * сторінці часу.
 *
 * Файл лишається заглушкою, бо маршрут зареєстрований у `app/_layout.tsx` і на
 * нього ще ведуть посилання з інших екранів (меню вкладки «Завдання»),
 * нотифікації та закладки. Видалити його означало б віддати людині порожній
 * стек навігації замість екрана.
 */
import { Redirect } from 'expo-router';
import React from 'react';

export default function TimeRecordsRedirect() {
  return <Redirect href="/(tabs)/time" />;
}
