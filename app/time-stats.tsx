/**
 * app/time-stats.tsx — редирект на «Час».
 *
 * Окремої «Статистики часу» більше немає: вона показувала розподіл за змінами
 * доби (ранок/день/вечір/ніч), а цього поділу в продукті не лишилось — за ним
 * не будувався жоден звіт. Підсумки, заради яких сюди заходили, тепер у блоці
 * KPI на самій сторінці часу, з розподілом за ПРОЄКТАМИ.
 *
 * Заглушка, а не видалений файл: маршрут зареєстрований у `app/_layout.tsx`, і
 * без неї перехід віддав би порожній стек навігації.
 */
import { Redirect } from 'expo-router';
import React from 'react';

export default function TimeStatsRedirect() {
  return <Redirect href="/(tabs)/time" />;
}
